class Dialogs {
    _questions = [];
    _dialogs = [];

    addHuman(name, timer) {
        this._dialogs.push("");
    }

    writeQuestions(numberQuestion, number, timer) {

    }
}

export default Dialogs